{
  "name": "nexora-core-workspace",
  "version": "1.0.0",
  "description": "Production network routing node server for Nexora Matrix",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pnpm run --filter * build || vite build || echo 'No build needed'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
